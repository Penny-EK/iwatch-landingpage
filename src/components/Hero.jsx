import HeroText from "./HeroText";
import Gallery from "./Gallery";

const Hero = () => {
  return (
    <section className="grid3-1 hero innerSection">
      <HeroText />
      <Gallery />
    </section>
  );
};

export default Hero;
